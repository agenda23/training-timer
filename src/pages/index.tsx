import Timer from '@/components/Timer'

export default function Home() {
  return (
    <main style={{ margin: 0, padding: 0, width: '100%', height: '100%', overflow: 'hidden' }}>
      <Timer />
    </main>
  )
} 